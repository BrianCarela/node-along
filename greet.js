const getInput = require("./get-input.js");

// 1. get the user's input and save it in a variable
const str = getInput();

// 2. Print it back out to the console based on user input
console.log("Sup " + str + "!")